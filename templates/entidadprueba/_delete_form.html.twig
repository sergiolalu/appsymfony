<form method="post" action="{{ path('app_entidadprueba_delete', {'id': entidadprueba.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entidadprueba.id) }}">
    <button class="btn">Delete</button>
</form>
